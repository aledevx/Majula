@model IEnumerable<Pk.Models.Equipamento>

@{
    ViewData["Title"] = "Equipamentos";
}


<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>

<body>
    <div style="display: flex; justify-content: space-between; align-items: center;">
        <p>
            <a asp-action="Criar" class="botao-add"><i class="fas fa-plus"></i></a>
        </p>
        <form asp-controller="Equipamentos" asp-action="Index" method="get">
            <p>
                Title: <input type="text" name="SearchString" />
                <input type="submit" value="Filter" />
            </p>
        </form>
    </div>

    <table class="table table-striped">
        <thead class="thead-computador">
            <tr>
                <th style="color: #303030;">
                    @Html.DisplayNameFor(model => model.Tombamento)
                </th>
                <th style="color: #fff;">
                    @Html.DisplayNameFor(model => model.Categoria)
                </th>
                <th style="color: #fff;">
                    @Html.DisplayNameFor(model => model.Descricao)
                </th>
                <th style="color: #fff;">
                    @Html.DisplayNameFor(model => model.Status)
                </th>
                <th>

                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Tombamento)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Categoria)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Descricao)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Status)
                    </td>
                    <td>
                        <a asp-action="Detalhes" class="botao-visualizar btn" asp-route-id="@item.Id">Detalhes</a>
                        @* <a asp-action="Editar" asp-route-id="@item.Id">Editar</a>
                            <a asp-action="Excluir" asp-route-id="@item.Id">Excluir</a> *@
                    </td>
                </tr>
            }
        </tbody>
    </table>
</body>
@if (@ViewBag.equipamentoNaoEncontrado == true)
{
    <div class="col-md-12">
        <br>
        <div class="row">
            <div class="alert alert-danger" role="alert" style="margin: auto; opaticy: 0.5;">
                <h4 class="alert-heading">EQUIPAMENTO NÃO ENCONTRADO!</h4>
                <p>Verifique se o <b>Tombamento</b> do equipamento foi digitado corretamente.</p>
            </div>
        </div>

    </div>
}
else
{

}